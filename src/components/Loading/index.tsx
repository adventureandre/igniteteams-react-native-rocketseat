import styled from "styled-components"
import { Container, LoadIndicator } from "./styles"

export const Loading = ()=>{
    return (
        <Container>
            <LoadIndicator/>
        </Container>
    )
}

